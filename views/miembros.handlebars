<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>MIEMBROS DEL EQUIPO</h1>
  </header>
  <main>
    <ul>
      {{#each miembros}}
        <li>
          <div class="centered-image-container">
            <img src="{{imagen_url}}" alt="Foto de {{nombre}} {{apellidos}}" class="member-image">
          </div>
          <h2>{{nombre}} {{apellidos}}</h2>
          <p><strong>Titulación:</strong> {{titulacion}}</p>
          <p><strong>Tecnologías:</strong> {{tecnologias}}</p>
          <p><strong>Idiomas:</strong> {{idiomas}}</p>
          <a href="/miembros/{{nombre}}" class="button">Ver Perfil</a>
        </li>
        <hr>
      {{/each}}
    </ul>
    <a href="/" class="button">Volver a la página principal</a>
  </main>
</body>
</html>
