
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{titulo}}</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Miembros del Equipo</h1>
  <ul class="lista-miembros">
    {{#each miembros}}
      <li class="elemento-miembro">
        <img src="{{imagen_url}}" alt="Foto de {{nombre}} {{apellidos}}" class="imagen-miembro">
        <br>
        <strong>{{nombre}} {{apellidos}}</strong> - {{titulacion}}<br>
        <em>Tecnologías:</em> {{tecnologias}}<br>
        <em>Idiomas:</em> {{idiomas}}<br>
        <em>Proyectos Personales:</em> {{proyectos_personales}}
      </li>
      <hr>
    {{/each}}
  </ul>

  <a href="/">Volver a la página principal</a>
</body>
</html>